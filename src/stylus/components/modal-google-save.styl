.modal-google-save
  border none
  .google-save-panel
    label-font()
    max-width: 400px
    padding 1em
    color black
    input
      margin 1em
    input[type=text]
      font-size 1em
      padding 0.3em
      background-color: lighten(light-blue, 70%)
      border
    .buttons
      float: right
    button
      modal-button($thickness:12px)
      margin 1em
